## Supabase (required for fund API fallback + login + database)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

## Python backend (AKShare + OCR). If empty, frontend falls back to Supabase Edge Function.
VITE_BACKEND_URL=http://localhost:8001

